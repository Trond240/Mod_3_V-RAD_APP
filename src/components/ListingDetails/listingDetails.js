import React from 'react';

export const ListingDetails = (props) => {
    // console.log(props)
const allDetails = props.listings.map(listing => {
        console.log(listing)
    })
    return(
        <section>Made it</section>
    )
}